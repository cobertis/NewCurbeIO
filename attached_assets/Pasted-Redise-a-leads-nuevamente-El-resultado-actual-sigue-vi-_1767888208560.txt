Rediseña /leads nuevamente. El resultado actual sigue viéndose vacío y amateur por el “big empty state” centrado y exceso de whitespace. Quiero un CRM denso y operativo. NO quiero una pantalla de onboarding.

CONTRA-EJEMPLO (lo que NO quiero):
- Un card gigante con “No leads yet” en el centro ocupando toda la altura.
- Dos paneles vacíos (sidebar vacío + main vacío) con iconitos y aire.

OBJETIVO:
- Layout tipo “workbench”: estructura estable, tabla siempre visible, densidad alta, acciones claras.
- Estado vacío debe ser INLINE y compacto.

REQUISITOS DE LAYOUT (exactos):
1) Top bar sticky (h-14):
   - Izq: “Leads” + contador
   - Centro: Search (max-w 520)
   - Der: [Import CSV] primary, [Create Lead] secondary, Batch dropdown
   - Debajo del top bar: KPI chips (Total, Contactable, DNC Blocked, No Valid Phone, Valid Email) en una sola fila compacta (no ocupa altura extra grande).

2) Main grid (desktop):
   - Grid de 12 columnas, gap-4, max-w-screen-2xl, px-6.
   - Sidebar batches: col-span-3 (min-w ~280), height: calc(100vh - topbar - padding), scroll interno.
   - Main leads: col-span-9, height: calc(100vh - topbar - padding), scroll interno SOLO en la tabla.

3) Sidebar “Import Batches” (NO empty gigante):
   - Header compacto: título + contador + botón pequeño “Import”.
   - Body:
     - Si no hay batches: mostrar SOLO una fila compacta (2 líneas) + botón Import (sm). Nada de iconos enormes.
     - Si hay batches: lista densa (cada item h-12/h-14) con #leads y %contactable.

4) Main card “Leads Table”:
   - Header row dentro del card:
     - Search ya está en top bar, así que aquí solo: botón [Filters] y sort dropdown y “Active filters” chips (si existen).
   - Tabla SIEMPRE renderizada con headers aunque no haya data.
   - Mostrar 8-12 skeleton rows cuando:
     a) loading
     b) empty (para que el layout no se vea muerto)

5) Estado vacío (inline, no centrado):
   - Dentro del body de la tabla, arriba:
     “No leads yet. Import a CSV to start.”
     [Import CSV] [Download sample]
     Checklist pequeño en 2 columnas.
   - NO centrar verticalmente. NO ocupar toda la altura.

6) Densidad / spacing (obligatorio):
   - Eliminar min-h gigantes. Nada de “py-24” ni “h-[600px]” para empty states.
   - Card padding: p-4 o p-5, no más.
   - Tabla row height: 44–52px.
   - Reducir whitespace general (gap-4/6 máximo).
   - Evitar grandes “hero blocks”.

7) Estilo visual:
   - Sidebar con background sutil, border y shadow mínimo.
   - Tabla con sticky header.
   - Hover/selected states en filas.
   - Truncate + tooltip en textos largos.

IMPLEMENTACIÓN TÉCNICA:
- Crear componentes: LeadsTopbar, LeadsKPIs, BatchesSidebar, LeadsTableCard, FiltersSheet, EmptyInlineNotice, LeadDetailDrawer.
- Ajustar layout con Tailwind:
  - wrapper: `max-w-screen-2xl mx-auto px-6 py-4`
  - grid: `grid grid-cols-12 gap-4`
  - panes: `h-[calc(100vh-112px)]` (ajusta según altura real topbar+kpis)
  - panes: `overflow-hidden`, y dentro: `overflow-auto`
- Reemplazar el empty state actual por EmptyInlineNotice dentro del body de la tabla.

CRITERIOS DE ACEPTACIÓN:
- La pantalla NO se ve vacía aunque no haya leads: tabla visible + sidebar compacta + estructura firme.
- No hay iconos gigantes ni mensajes centrados ocupando media pantalla.
- Import CSV siempre a mano y no duplicado innecesariamente.
- Con 1000+ leads el layout se mantiene usable (sticky header y scroll en tabla).
