TAREA: Campaign Orchestrator Blueprint (Docs + Contracts)

OBJETIVO:
- Crear un documento de especificación que describa el “Journey Orchestrator” para campañas multicanal (SMS/iMessage/WhatsApp/Voice/Voicemail/RVM) con IA recomendando el siguiente paso, pero con Policy Engine determinístico mandando.
- NO escribir código todavía. SOLO documentación y contratos JSON.

ALCANCE (IN):
- Definir estados del journey, eventos normalizados, esquema de “AllowedAction” y “NextAction”, stop rules, caps, quiet hours.
- Definir contratos de endpoints internos/externos que construiremos en los siguientes tickets.

ALCANCE (OUT):
- No migraciones, no endpoints reales, no UI.

INSTRUCCIONES:
1) Inspecciona el repo actual y crea una carpeta /docs si no existe.
2) Crea el archivo: /docs/campaign_orchestrator.md
3) Incluye estas secciones (obligatorio):
   A) Visión y objetivos (1 página)
   B) Estados por contacto:
      - NEW, ATTEMPTING, ENGAGED, QUALIFIED, BOOKED, NOT_INTERESTED, STOPPED, UNREACHABLE, DO_NOT_CONTACT
   C) Eventos normalizados (tabla):
      - MESSAGE_SENT, MESSAGE_DELIVERED, MESSAGE_FAILED, MESSAGE_REPLIED,
        CALL_PLACED, CALL_ANSWERED, CALL_NO_ANSWER, CALL_FAILED,
        VOICEMAIL_DROPPED, RVM_DROPPED,
        OPT_OUT, COMPLAINT, MANUAL_STOP, TIMEOUT
   D) Stop rules (hard rules):
      - OPT_OUT/STOP -> DO_NOT_CONTACT y detener todo
      - COMPLAINT -> detener todo y marcar riesgo alto
      - max attempts total por campaign_contact
      - caps por 24h
   E) Quiet hours (por zona horaria): ejemplo 9am-8pm local.
   F) Esquema “AllowedAction” (JSON schema aproximado):
      - channel, target (number/handle), reason, cost_estimate, constraints_applied
   G) Esquema “NextAction” (JSON schema aproximado):
      - chosen_action, message_template_id o message_body, wait_seconds, explanation
   H) Policy Engine (determinístico): entradas/salidas exactas.
   I) Orchestrator loop (worker): algoritmo en pseudocódigo.
   J) Channel Router: interfaz de adaptadores y outcomes estándar.
   K) Auditoría: qué se loggea por evento y por decisión.

CRITERIO “DONE” (NO NEGOCIABLE):
- [ ] Existe /docs/campaign_orchestrator.md con todas las secciones.
- [ ] Incluye ejemplos de JSON para AllowedAction y NextAction.
- [ ] No se escribió código, solo docs.

SALIDA:
- Devuélveme el contenido del doc y la ruta del archivo creado.
